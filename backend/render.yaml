services:
  - type: web
    name: subjectify-backend
    env: python
    plan: free
    region: oregon
    rootDir: backend  # 👈 Add this line
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    autoDeploy: true
